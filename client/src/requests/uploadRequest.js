export const buildInitiateUploadRequest = (fileName,fileSize,mimeType,ownerId,chunks) =>{
    fileName,
    fileSize,
    mimeType,
    ownerId,
    chunks
}

export const buildChunkRequest = (fileId,index,hash,size) =>{
    fileId,
    index,
    hash,
    size
}